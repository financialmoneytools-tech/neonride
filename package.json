{
  "name": "neon-ride",
  "version": "0.0.0",
  "description": "Neon Ride - a first-person neon motorcycle ride, in the browser.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev:phone": "vite --config vite.phone.config.js --host",
    "dev:tunnel": "vite --config vite.tunnel.config.js --host",
    "smoke": "node tools/smoke-mobile.mjs"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "playwright": "^1.63.0",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "three": "^0.186.0"
  }
}
